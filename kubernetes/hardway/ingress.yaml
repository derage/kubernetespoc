apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: kpoc-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: kpoc
          servicePort: 80
